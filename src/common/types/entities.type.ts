export type VehicleType = {
  id: number;
  title: string;
  usages: VehicleUsage[];
};

export type VehicleUsage = {
  id: number;
  title: string;
};

export type InsuranceCompany = {
  id: number;
  title: string;
};

export type ThirdDiscount = {
  id: number;
  title: string;
};
